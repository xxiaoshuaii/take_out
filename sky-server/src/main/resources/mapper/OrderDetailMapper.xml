<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail (order_id, dish_id, setmeal_id, name, dish_flavor, number, amount, image) values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.setmealId},
            #{item.name},#{item.dishFlavor},#{item.number},
            #{item.amount},#{item.image})
        </foreach>
    </insert>

    <select id="getSalesTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name,sum(od.number) as number from order_detail od,orders o
        <where>
            o.status = 5
            and o.id = od.order_id
            <if test="begin != null">
                and o.order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and o.order_time &lt;= #{end}
            </if>
        </where>
        group by od.name
        order by number desc
        limit 0,10;
    </select>
</mapper>